// The auth middleware haven't been created in this example because no login is needed.
// This middleware would be imported in the routes files and added to the routes that require authentification.